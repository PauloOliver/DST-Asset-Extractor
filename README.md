# DST Asset Extractor

A simple Python tool to extract sprites from Don't Starve Together.

## Features

- Extracts all ZIP files from the game's `anim` folder
- Converts `.tex` files to `.png`
- Preserves the original folder structure
- One-click execution